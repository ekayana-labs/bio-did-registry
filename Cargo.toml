[workspace]
members = ["program"]
resolver = "2"

[workspace.package]
authors = ["Ekayana Labs"]
edition = "2021"
rust-version = "1.89.0"
license = "MIT"
repository = "https://github.com/ekayana-labs/did-bio-registry"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1

[profile.release.build-override]
opt-level = 3
incremental = false
codegen-units = 1
